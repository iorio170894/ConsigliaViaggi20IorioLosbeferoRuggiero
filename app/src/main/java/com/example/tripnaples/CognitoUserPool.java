package com.example.tripnaples;

import com.amazonaws.ClientConfiguration;

public class CognitoUserPool {

    // imposta la configurazione del servizio AWS. Scelta della configurazione predefinita
    ClientConfiguration clientConfiguration = new ClientConfiguration ();
    
    // Crea un oggetto CognitoUserPool per fare riferimento al pool di utenti
   // CognitoUserPool userPool = new CognitoUserPool (context, poolId, clientId, clientSecret, clientConfiguration);

}
